# Local data cache (XTracker posts + CLOB prices + trades + resolved-market metadata)
data/cache.db
data/cache.db-shm
data/cache.db-wal

# Local tooling / editor config
.claude/

# All collected backtest / post-mortem / order-book data (large, regenerable). See backtest_data/README.md
backtest_data/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS / editor
.DS_Store

# Personal wallet address (local only)
data/wallet.txt
